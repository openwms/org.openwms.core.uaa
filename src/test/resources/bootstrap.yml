spring:
  config:
    name:
  cloud:
    config:
      label:
      discovery:
        enabled: false
        service-id:
  security:
    user:
      # Override this in cloud environment!
      password: sa


owms:
  eureka:
    # Override this in cloud environment!
    url:

system:
  user: openwms
  password: openwms

server:
  port: ${PORT:8085}

eureka:
  client:
    serviceUrl:
      defaultZone:
  instance:
    metadata-map:
      username:
      password:
      protocol:
