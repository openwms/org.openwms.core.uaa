eureka:
  client:
    healthcheck:
      enabled: true

owms:
  security:
    encoder:
      bcrypt:
        strength: 15
    useEncoder: false
    successUrl: "/"
    system:
      username: openwms
      password: openwms