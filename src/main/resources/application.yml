eureka:
  client:
    healthcheck:
      enabled: true

owms:
  security:
    encoder:
      bcrypt:
        strength: 4
    successUrl: "/"
    system:
      username: openwms
      password: openwms